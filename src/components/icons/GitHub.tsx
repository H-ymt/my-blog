import { GithubIcon } from "lucide-react";

export default function Github() {
  return (
    <div className="btn btn-ghost btn-sm px-2 text-primary">
      <GithubIcon size={20} strokeWidth={1.5} />
    </div>
  );
}
